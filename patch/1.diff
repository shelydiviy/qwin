diff --git a/src/main.cpp b/src/main.cpp
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -1,5 +1,24 @@
 #include "server.h"
+#include <thread>
+#include <vector>
+#include <asio.hpp>
+
+void worker(asio::io_context& ioContext) {
+    ioContext.run();
+}
+
+void initializeThreadPool(asio::io_context& ioContext, size_t numThreads) {
+    std::vector<std::thread> threads;
+    for (size_t i = 0; i < numThreads; ++i) {
+        threads.emplace_back(worker, std::ref(ioContext));
+    }
+
+    for (auto& t : threads) {
+        if (t.joinable()) {
+            t.join();
+        }
+    }
+}
+
 int main() {
     // Инициализация пула потоков
     asio::io_context ioContext;
@@ -7,6 +26,7 @@ int main() {
         logMessage("Не удалось определить количество ядер процессора. Используется 1 поток.");
         numThreads = 1;
     }
+    initializeThreadPool(ioContext, numThreads);

     // Создание и запуск серверов
     Server server;
diff --git a/src/server.cpp b/src/server.cpp
--- a/src/server.cpp
+++ b/src/server.cpp
@@ -1,3 +1,25 @@
+#include "server.h"
+#include <unordered_set>
+#include <string>
+
+std::unordered_set<std::string> blockedIPs;
+
+bool Server::isIPBlocked(const std::string& ip) {
+    return blockedIPs.find(ip) != blockedIPs.end();
+}
+
+void Server::blockIP(const std::string& ip) {
+    blockedIPs.insert(ip);
+    logMessage("IP-адрес " + ip + " заблокирован", "info");
+}
+
+void Server::handleIncomingConnection(const asio::ip::udp::endpoint& endpoint) {
+    std::string clientIP = endpoint.address().to_string();
+    if (isIPBlocked(clientIP)) {
+        logMessage("Подключение от заблокированного IP: " + clientIP, "warning");
+        return;
+    }
+    // Обработка подключения
+}

 void Server::start() {
     // Существующий код...
diff --git a/src/server.h b/src/server.h
--- a/src/server.h
+++ b/src/server.h
@@ -1,3 +1,8 @@
+#ifndef SERVER_H
+#define SERVER_H
+
 class Server {
 public:
     void start();
+    bool isIPBlocked(const std::string& ip);
+    void blockIP(const std::string& ip);
+    void handleIncomingConnection(const asio::ip::udp::endpoint& endpoint);
 };
+
+#endif // SERVER_H
diff --git a/src/steam.cpp b/src/steam.cpp
--- a/src/steam.cpp
+++ b/src/steam.cpp
@@ -1,3 +1,39 @@
+#include "steam.h"
+#include <random>
+#include <ctime>
+#include <asio.hpp>
+
+uint32_t Steam::generateChallengeKey() {
+    std::random_device rd;
+    std::mt19937 gen(rd());
+    std::uniform_int_distribution<uint32_t> dis(0, UINT32_MAX);
+    return dis(gen);
+}
+
+void Steam::handleChallengeRequest(asio::ip::udp::socket& socket, const asio::ip::udp::endpoint& masterEndpoint, uint32_t challenge) {
+    ChallengePacket packet;
+    packet.challenge = challenge;
+
+    std::vector<uint8_t> buffer(sizeof(ChallengePacket));
+    memcpy(buffer.data(), &packet, sizeof(ChallengePacket));
+
+    socket.async_send_to(
+        asio::buffer(buffer), masterEndpoint,
+        [](std::error_code ec, [[maybe_unused]] std::size_t /*bytesSent*/) {
+            if (ec) logMessage("Ошибка отправки challenge-ключа: " + ec.message(), "error");
+            else logMessage("Challenge-ключ успешно отправлен", "info");
+        });
+}
+
+void Steam::sendServerUpdate(const asio::ip::udp::endpoint& masterEndpoint) {
+    std::vector<uint8_t> buffer;
+    // TODO: Заполнить buffer данными о состоянии сервера
+
+    socket.async_send_to(
+        asio::buffer(buffer), masterEndpoint,
+        [](std::error_code ec, [[maybe_unused]] std::size_t /*bytesSent*/) {
+            if (ec) logMessage("Ошибка отправки обновления состояния: " + ec.message(), "error");
+        });
+}
+
 void Steam::start() {
     // Существующий код...
 }
diff --git a/src/steam.h b/src/steam.h
--- a/src/steam.h
+++ b/src/steam.h
@@ -1,3 +1,18 @@
+#ifndef STEAM_H
+#define STEAM_H
+
+#include <asio.hpp>
+
+struct ChallengePacket {
+    uint8_t header = 0x69; // Заголовок пакета
+    uint32_t challenge;   // Challenge-ключ
+};
+
 class Steam {
 public:
+    uint32_t generateChallengeKey();
+    void handleChallengeRequest(asio::ip::udp::socket& socket, const asio::ip::udp::endpoint& masterEndpoint, uint32_t challenge);
+    void sendServerUpdate(const asio::ip::udp::endpoint& masterEndpoint);
     void start();
 private:
     asio::ip::udp::socket socket;
+#endif // STEAM_H
